apiVersion: v1
kind: Service
metadata:
  name: fortigate-mcp-server
  namespace: kagent
  labels:
    app: fortigate-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: fortigate-mcp-server
